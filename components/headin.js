import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function headin(children) {
  return (
    
      <Text>headin</Text>
    
  )
}

const styles = StyleSheet.create({

heading: {
    fontsize: 20,
    fontWeight: 'bold',
    textAlign: 'center',
    borderEndWidth: 1,
    borderEndColor: "#000",
    padding: 12
},

});